import 'package:flutter/material.dart';

const kMainColor = Color(0xFF778D45);
const kAccentColor = Colors.white;
const kPaddingContainer = EdgeInsets.symmetric(horizontal: 20.0);
const kContainerDecoration = BoxDecoration(
  borderRadius: BorderRadius.only(
    topLeft: Radius.circular(20.0),
    topRight: Radius.circular(20.0),
  ),
  color: kAccentColor,
);
const kModalBackgroundColor = Color(0xFFAEC09A);
